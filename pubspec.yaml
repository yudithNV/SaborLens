name: saborleans
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  google_fonts: ^6.2.1
  http: ^1.2.2
  http_parser: ^4.1.2
  image_picker: ^1.1.2
  flutter:
    sdk: flutter
  get: ^4.7.3
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
